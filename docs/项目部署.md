## 一、如果没有docker环境

安装解压项目代码

![image-20260809231043733](C:\Users\ChenHM\AppData\Roaming\Typora\typora-user-images\image-20260809231043733.png)

安装uv：

```bash
pip install uv
```

如果有gpu环境，安装onnxruntion-gpu[cuda,cudann]>=1.28.0； 没有gpu环境，安装onnxruntime

![image-20260809231757436](C:\Users\ChenHM\AppData\Roaming\Typora\typora-user-images\image-20260809231757436.png)

```bash
# 运行命令
uv sync
uv run python -m face_api.main --live-stream-url rtsp://localhost:8554/camera --live-rtsp-transport tcp --live-match-threshold 0.05 --live-diagnostics-enabled  --video-analysis-download-source 
```

## 二、如果有docker环境



```bash
docker compose up -d --build # 无gpu环境启动
docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d --build # gpu启动
```

